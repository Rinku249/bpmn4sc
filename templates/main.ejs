template <%= parent.id %> 
    with
        <%= parent.documentation %>
    where
        signatory <%= parent.signatory %>
        
        nonconsuming choice <%= parent.name.replaceAll(" ","") %> : ()
            controller <%= parent.signatory %>
            do
                return ()
        